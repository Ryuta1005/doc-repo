# 手動テストケース・手順書（Story 005）

## 実施前チェック

- [ ] `npm install` 済み
- [ ] `npm run build` が成功
- [ ] テスト対象ディレクトリに `.md` が1件以上ある
- [ ] 既存の `doc-repo serve` が停止している

## テスト環境情報

- 実施日:
- 実施者:
- OS:
- Node.js:
- ブランチ: `005-local-server-serve`

## 判定基準

- Pass: 期待結果をすべて満たす
- Fail: 期待結果を1つでも満たさない
- Blocked: 前提不備で実行不能

## ケース一覧

| ID     | 目的                                  | 優先度 |
| ------ | ------------------------------------- | ------ |
| MT-001 | 初回生成から配信開始までのE2E成立確認 | High   |
| MT-002 | デフォルトポート確認                  | High   |
| MT-003 | 設定ファイルポート確認                | High   |
| MT-004 | CLI優先順位確認                       | High   |
| MT-005 | 初回生成失敗時のFail Fast確認         | High   |
| MT-006 | ポート競合時のエラー確認              | High   |
| MT-007 | 不正port入力検証                      | Medium |
| MT-008 | 停止と再起動確認                      | Medium |

---

## MT-001 初回生成から配信開始までのE2E成立確認

目的:

- 生成物未作成状態でも `serve` が初回生成を先に行い、配信開始できることを確認する。

手順:

1. `rm -rf .doc-repo`
2. `node dist/cli/index.js serve`
3. 標準出力で以下の状態を確認する。

- 初回生成成功
- 配信URL表示
- 監視開始表示

4. ブラウザで表示URLへアクセスする。

期待結果:

- 初回生成後にサーバーが起動する
- URLにアクセスするとサイトが表示される

実測:

- 実行結果:
- 判定:
- 補足:

## MT-002 デフォルトポート確認

目的:

- `port` 未指定時に `4000` で起動することを確認する。

手順:

1. `doc-repo.config.json` が存在する場合は退避する。
2. `node dist/cli/index.js serve`
3. 出力URLのポート番号を確認する。

期待結果:

- `http://localhost:4000` で起動する

実測:

- 実行結果:
- 判定:
- 補足:

## MT-003 設定ファイルポート確認

目的:

- 設定ファイル `port` が起動ポートとして適用されることを確認する。

手順:

1. 以下を作成する。

```json
{
  "port": 4100
}
```

2. `node dist/cli/index.js serve`
3. 出力URLを確認する。

期待結果:

- `http://localhost:4100` で起動する

実測:

- 実行結果:
- 判定:
- 補足:

## MT-004 CLI優先順位確認

目的:

- `CLI > 設定ファイル > デフォルト` の優先順位を確認する。

手順:

1. `doc-repo.config.json` に `{"port": 4100}` を設定する。
2. `node dist/cli/index.js serve --port 4500`
3. 出力URLを確認する。

期待結果:

- `http://localhost:4500` で起動する

実測:

- 実行結果:
- 判定:
- 補足:

## MT-005 初回生成失敗時のFail Fast確認

目的:

- 初回生成失敗時にサーバー未起動で終了コード `1` となることを確認する。

手順:

1. `doc-repo.config.json` に以下を設定する。

```json
{
  "rootDir": "./not-found-dir"
}
```

2. `node dist/cli/index.js serve`
3. 終了コードを確認する。

期待結果:

- 初回生成失敗の理由が表示される
- サーバーは起動しない
- 終了コード `1`

実測:

- 実行結果:
- 判定:
- 補足:

## MT-006 ポート競合時のエラー確認

目的:

- ポート競合時に理由付きメッセージで終了コード `1` になることを確認する。

手順:

1. `node -e "require('node:http').createServer(()=>{}).listen(4000)" &`
2. `node dist/cli/index.js serve`
3. エラー表示と終了コードを確認する。
4. 占有プロセスを終了する。

期待結果:

- ポート競合メッセージが表示される
- 終了コード `1`

実測:

- 実行結果:
- 判定:
- 補足:

## MT-007 不正port入力検証

目的:

- 不正な `port` 入力を起動前に検出することを確認する。

手順A（型不正）:

1. `doc-repo.config.json` に `{"port": "abc"}` を設定する。
2. `node dist/cli/index.js serve`

手順B（範囲外）:

1. `doc-repo.config.json` に `{"port": 70000}` を設定する。
2. `node dist/cli/index.js serve`

期待結果:

- 対象フィールドと理由が分かるメッセージ
- いずれも終了コード `1`

実測:

- 手順A結果:
- 手順B結果:
- 判定:
- 補足:

## MT-008 停止と再起動確認

目的:

- Ctrl+C で停止し、同一ポートで再起動できることを確認する。

手順:

1. `node dist/cli/index.js serve`
2. 起動後に Ctrl+C を送る。
3. 同じコマンドを再実行する。

期待結果:

- 停止できる
- 再起動が成功する

実測:

- 実行結果:
- 判定:
- 補足:

---

## 総合判定

- 実行ケース数:
- Pass:
- Fail:
- Blocked:
- 総合判定:
- 次アクション:
