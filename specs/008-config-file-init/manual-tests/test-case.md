# 手動テストケース・手順書（Story 008）

## 事前確認

- [x] `npm install` 実行済み
- [x] `npm run build` 実行済み
- [x] `node dist/cli/index.js --help` が実行できる
- [x] 検証用ディレクトリを新規作成できる

## テスト環境情報

- 実施日:
- 実施者:
- OS:
- Node.js:
- ブランチ: 008-config-file-init

## Pass/Fail 判定基準

- Pass: 期待結果をすべて満たす
- Fail: 期待結果を1つ以上満たさない
- Blocked: 前提不足または環境制約で実施不能

## ケース一覧

| ID     | 目的                                                | 優先度 |
| ------ | --------------------------------------------------- | ------ |
| MT-801 | `init` で設定ファイルを新規作成できることを確認する | High   |
| MT-802 | 生成された設定ファイルのデフォルト値を確認する      | High   |
| MT-803 | 既存設定ファイルが上書きされないことを確認する      | High   |
| MT-804 | 書き込み不可時に失敗通知できることを確認する        | High   |
| MT-805 | 生成設定で通常生成が成立することを確認する          | High   |

---

## MT-801 init で設定ファイルを新規作成できることを確認する

目的:

- 空ディレクトリで `doc-repo init` を実行したときに、`doc-repo.config.json` が作成され、作成パスが標準出力に表示されることを確認する。

手順:

1. 任意の作業場所で検証ディレクトリを作成する。
2. そのディレクトリに移動する。
3. `node /Users/ryuta/Source/OSS/my_oss/doc-repo/dist/cli/index.js init` を実行する。
4. `ls -la` で `doc-repo.config.json` の存在を確認する。

期待結果:

- `doc-repo.config.json` が作成される。
- 標準出力に「設定ファイルを作成しました: <絶対パス>」が表示される。
- 終了コードが `0` である。

実測:

- 実行結果:
- 判定:Pass
- 補足:

## MT-802 生成された設定ファイルのデフォルト値を確認する

目的:

- 生成された `doc-repo.config.json` に必須キーとデフォルト値が含まれることを確認する。

手順:

1. MT-801 で作成した `doc-repo.config.json` を開く。
2. `rootDir`、`include`、`exclude`、`port` の有無を確認する。
3. 各値が `"."`、`["**/*.md"]`、`[]`、`4000` であることを確認する。

期待結果:

- 必須キー4項目がすべて存在する。
- 各デフォルト値が契約どおりである。

実測:

- 実行結果:
- 判定:Pass
- 補足:

## MT-803 既存設定ファイルが上書きされないことを確認する

目的:

- 既存 `doc-repo.config.json` がある状態で `init` を再実行しても、内容が変更されないことを確認する。

手順:

1. `doc-repo.config.json` の内容を任意に編集し、識別しやすい値（例: `port: 4100`）を設定する。
2. 再度 `node /Users/ryuta/Source/OSS/my_oss/doc-repo/dist/cli/index.js init` を実行する。
3. 実行前後で `doc-repo.config.json` の内容が同一か確認する。

期待結果:

- ファイル内容が上書きされない。
- 標準出力に「設定ファイルは既に存在します: <絶対パス>」が表示される。
- 終了コードが `0` である。

実測:

- 実行結果:
- 判定:Pass
- 補足:

## MT-804 書き込み不可時に失敗通知できることを確認する

目的:

- 書き込み権限がないディレクトリで `init` を実行した場合、理由付きエラー表示と終了コード `1` になることを確認する。

手順:

1. 一時ディレクトリを作成する（例: `mkdir -p /tmp/doc-repo-init-no-write`）。
2. 権限を書き込み不可に変更する（例: `chmod 555 /tmp/doc-repo-init-no-write`）。
3. 対象ディレクトリに移動する。
4. `node /Users/ryuta/Source/OSS/my_oss/doc-repo/dist/cli/index.js init` を実行する。
5. 実行後に権限を戻す（例: `chmod 755 /tmp/doc-repo-init-no-write`）。

期待結果:

- 標準エラー出力に「エラー: 設定ファイルの作成に失敗しました。」と理由行が表示される。
- 終了コードが `1` である。
- `doc-repo.config.json` は作成されない。

実測:

- 実行結果:
- 判定:Pass
- 補足:

## MT-805 生成設定で通常生成が成立することを確認する

目的:

- `init` で生成した設定ファイルをそのまま利用して通常生成が完了することを確認する。

手順:

1. MT-801 の検証ディレクトリ配下に Markdown ファイルを1件作成する（例: `README.md`）。
2. `node /Users/ryuta/Source/OSS/my_oss/doc-repo/dist/cli/index.js` を実行する。
3. 出力先 `.doc-repo` 配下に HTML が生成されることを確認する。

期待結果:

- 通常生成がエラーなく完了する。
- `.doc-repo` 配下に生成物が出力される。
- 終了コードが `0` である。

実測:

- 実行結果:
- 判定:
- 補足:

---

## 結果記録フィールド

- ケースID:
- 実施日時:
- 実施者:
- コマンド:
- 標準出力要点:
- 標準エラー要点:
- 終了コード:
- 判定:
- 補足:

---

## 総合判定

- 実行ケース数: 5
- Pass:
- Fail:
- Blocked:
- 総合判定:
- 次アクション:
