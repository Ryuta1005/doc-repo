# 生成者はローカルサーバーでドキュメントサイトを起動できる。なぜならワンコマンドでブラウザからサイトを確認できるようにしたいから。

## メタ情報

- 種別: Story
- 番号: 005
- Epic: 004
- ステータス: Done
- 優先度: P1
- Spec: 005-local-server-serve
- Depends On: -

## ユーザー価値

`doc-repo serve` を実行するだけで、ブラウザでドキュメントサイトを確認できる。

## 受け入れ条件

- `doc-repo serve` コマンドでローカル HTTP サーバーが起動する
- デフォルトのポートは `4000`（`http://localhost:4000`）とする
- 起動後にアクセス可能な URL が標準出力に表示される
- ブラウザでその URL にアクセスするとドキュメントサイトが表示される
- `doc-repo.config.json` の `port` フィールドでポート番号を変更できる
- Ctrl+C でサーバーを停止できる
- ポートが競合している場合、理由が分かるメッセージを表示して終了コード 1 で終了する

## 実装タスク管理

- 機能実装タスクは Speckit のフローで管理する
